apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: kube-app
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: samplenginx
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 50
